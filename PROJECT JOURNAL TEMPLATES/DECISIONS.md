# 📋 Журнал архитектурных решений и состояния проекта (DECISIONS)

**Проект:** FlightSaver (Smart Split-Ticketing & Transit STPC/TWOV Flight Search Platform)  
**Дата обновления:** 2026-08-25  
**Версия документа:** v8.37.0  
**Статус проекта:** В разработке / Перевод FlightSaver на 100% живого ИИ-агента Gemini с полным рассуждением и устойчивостью к опечаткам.

---

## 1. Принятые ключевые решения (ADR)

### ADR-001 — ADR-049:
(Архитектура, Mock-режим, Мультивалютность, AI-First интерфейс, In-House чекаут, Консолидация в FlightSaver, Google-Style Apps Menu, Корректировка позиционирования агентской модели, Номера карточек, Горизонтальное меню, Инлайн-фиксация, Liquid Glass FLINEX, Минималистичный Hero-блок, Outfit, Zekton, Пространственная юстировка, Интерактивное меню Google, Чат с покупателем, 100% RU/EN паритет, Conversational Flow, Эластичная доступность, Supabase SSR схема, Дашборд со статистикой, Полный аудит типов, Мобильная шторка, Fixed Backdrop Bottom Sheet, Top-Right Close Alignment, Адаптивность доступности, Иерархия подвала, Центрирование подвала, Google OAuth Callback Route, Безопасная инициализация Supabase, Изоляция клиентских ошибок аутентификации, Фиксация боевых параметров проекта, Защита и санитизация URL, Удаление тестового пользователя, Канонический Header, Расширение Tailwind, Прямой переход через window.location.assign, PostCSS и Tailwind сборка, Единый модуль lib/auth.ts, Явная запись Cookie в NextResponse, Клиентская авторизация Google OAuth с PKCE, Клиентский обмен OAuth Code и очистка URL, Подключение реальных данных Supabase в Dashboard, Серверная защита маршрутов через middleware.ts).

### ADR-050: Отказоустойчивый Middleware с защитой try/catch
- **Контекст:** Устранить падение `500: MIDDLEWARE_INVOCATION_FAILED` при обработке запросов в Edge Runtime.
- **Решение:**
  - В `middleware.ts` внедрена правильная мутация `supabaseResponse` внутри `setAll`.
  - Весь код обернут в `try / catch` с безопасным пропуском `NextResponse.next({ request })` в случае непредвиденных сбоев, обеспечивая работу клиентского guard в `page.tsx`.
- **Статус:** Реализовано в v8.16.0.

### ADR-051: API-эндпоинт автодополнения аэропортов и городов (Duffel Places Suggestions API)
- **Контекст:** Реализовать высокопроизводительный серверный маршрут `/api/airports` для быстрого поиска и автодополнения городов и аэропортов по IATA-коду или названию.
- **Решение:**
  - Создан эндпоинт `app/api/airports/route.ts` с GET-обработчиком.
  - Добавлена валидация входного параметра `q` (ранний возврат пустого списка `{ places: [] }` при длине строки < 2).
  - Интегрирован вызов Duffel Places Suggestions API (`https://api.duffel.com/places/suggestions?query=...`) с авторизацией по токену `DUFFEL_ACCESS_TOKEN` и заголовком `Duffel-Version: v2`.
  - Реализован маппинг в унифицированный контракт `{ places: [{ id, name, iataCode, cityName, countryCode, type }] }`.
  - Добавлены интерфейсы `PlaceSuggestion` и `AirportSuggestionsResponse` в `lib/types.ts`.
- **Статус:** Реализовано в v8.17.0.

### ADR-052: Интеграция автодополнения городов и аэропортов в интерфейс поиска AIInputBar
- **Контекст:** Пользователю необходим плавный, визуально привлекательный и отзывчивый поиск городов и аэропортов с подсказками IATA-кодов прямо в основном поле ввода.
- **Решение:**
  - В компонент `components/AIInputBar.tsx` интегрировано плавающее выпадающее меню с подсказками локаций.
  - Настроен debounce 300 мс для оптимизации сетевых запросов.
  - Реализована полноценная поддержка навигации с клавиатуры (`ArrowUp`, `ArrowDown`, `Enter`, `Escape`) и закрытия по клику вне области (`click outside`).
  - Отображаются визуальные бейджи типов («Город» / «Аэропорт»), акцентные IATA-коды (`MOW`, `SVO`, `BKK`) и географическая привязка.
  - Добавлена нормализация и транслитерация русскоязычных названий городов в маршруте `app/api/airports/route.ts`.
- **Статус:** Реализовано в v8.18.0.

### ADR-053: Исправление сбоя 500 Internal Server Error на главной странице и оптимизация сборки
- **Контекст:** При переходе на `http://localhost:3000/` происходил сбой 500 из-за попытки Webpack разрешить относительные пути между дисками (C: и виртуальный Google Drive G:) и повреждений/неполноты файлов `styled-jsx` и PostCSS/шрифтов.
- **Решение:**
  - Во всех клиентских компонентах поиска и модальных окон гарантирована директива `'use client';` на 1-й строке.
  - Выполнена полная синхронизация модулей сборщика Next.js и `styled-jsx` в локальную директорию проекта.
  - Загрузка шрифтов Inter и Manrope переведена на надежный CSS `@import` в `globals.css` с указанием Google Fonts CDN и безопасных fallback-шрифтов, устраняя хрупкость загрузчика `next/font`.
  - Проверена корректная отдача `http://localhost:3000/` со статусом 200 OK и маршрутов `/api/airports` для русских и английских запросов.
- **Статус:** Реализовано в v8.19.0.

### ADR-054: Интеграция ИИ-парсера запросов на базе Google Gemini API (/api/search)
- **Контекст:** Реализовать серверный парсинг естественного языка на базе Google Gemini API для извлечения IATA-кодов городов, дат, пассажиров, багажа, STPC-отелей и генерации дружелюбного резюме `aiSummary`.
- **Решение:**
  - Обновлен эндпоинт `app/api/search/route.ts` (POST и GET) с интеграцией `process.env.GEMINI_API_KEY`.
  - Настроен структурированный системный промпт для моделей Gemini (`gemini-2.0-flash`, `gemini-1.5-flash`, `gemini-2.5-flash`, `gemini-3.6-flash`).
  - Разработан отказоустойчивый fallback с расширенной базой городов РФ и СНГ (склонения, падежи, IATA-коды) и грамматически корректной генерацией `aiSummary` на русском языке.
  - Главная страница `app/page.tsx` подключена к реальному вызову `/api/search` при нажатии Enter или кнопки «→».
  - В `components/FlightResultsList.tsx` реализовано интерактивное состояние загрузки («ИИ Консьерж анализирует маршрут и подбирает лучшие тарифы...») и вывод плашек с распознанными параметрами маршрута (IATA-коды, даты, пассажиры, багаж, отель STPC 4★).
- **Статус:** Реализовано в v8.20.0.

### ADR-055: Настройка режима полного авто-запуска (Always Allow / Auto Approve)
- **Контекст:** Обеспечить непрерывную и полностью автономную работу агента без пауз на подтверждение запуска терминальных команд, тестовых скриптов и редактирования файлов.
- **Решение:**
  - В корне проекта и в директории `FlightSaver` созданы/обновлены файлы `.vscode/settings.json` со свойствами:
    - `"antigravity.agent.autoExecution": "always"`
    - `"antigravity.agent.reviewPolicy": "alwaysProceed"`
    - `"antigravity.agent.terminal.autoApprove": true`
    - `"terminal.integrated.allowChaining": true`
  - Созданы файлы `.antigravityrules` с правилом полного авто-запуска.
  - Обновлен регламент `.agents/rules/rulesigorr.md` (пункт 4).
- **Статус:** Реализовано в v8.21.0.

### ADR-056: Полноценная логика ИИ Консьержа и динамический генератор билетов
- **Контекст:** Перевести поиск FlightSaver на полноценную адаптивную логику ИИ Консьержа: исключить показ одиночных подсказок аэропортов при сложных фразах, генерировать реалистичные билеты строго под заданный маршрут (а не заглушки Москвы) и поддерживать диалоговые уточнения `missingInfo`.
- **Решение:**
  - В `components/AIInputBar.tsx` отключен показ выпадающего списка аэропортов, если введено более одного слова (сложная поисковая фраза).
  - В `lib/nlpParser.ts` добавлены новые города (Дананг DAD, вариации Южно-Сахалинска UUS), парсинг точных дат (12 сентября -> 2026-09-12), классов обслуживания и распознавание последовательности городов при отсутствии предлогов.
  - Модуль `lib/mockFlights.ts` переведен на полноценную динамическую генерацию маршрутов: интеллектуальный подбор хабов (Ханой, Бангкок, Доха, Дубай, Пекин, Сеул, Стамбул), расчет агентских цен, скидки Split-Ticketing до 40% и времени стыковки с отелем STPC 4★.
  - В `components/FlightResultsList.tsx` обновлен текст лоадера («ИИ Консьерж подбирает стыковки и проверяет отели STPC...») и добавлена карточка с уточнениями от ИИ и кнопками быстрых ответов.
- **Статус:** Реализовано в v8.22.0.

### ADR-057: Система умного диалога с ИИ Консьержем и управление историей
- **Контекст:** Реализовать многошаговый диалог с ИИ Консьержем (контекст сессии, мгновенное очищение поля ввода, прокручиваемый интерфейс сообщений с автоскроллом и сохранение истории за 90 дней с лимитом 10 активных чатов).
- **Решение:**
  - В `app/page.tsx` добавлено состояние `conversationHistory`, поле ввода мгновенно очищается при отправке сообщения, а предыдущие параметры передаются в `/api/search`.
  - В `app/api/search/route.ts` и `lib/nlpParser.ts` реализовано удержание контекста: при частичных запросах (например, «10-30 сентября», «добавь 1 багаж», «на двоих») города маршрута сохраняются, обновляются только измененные параметры.
  - В `components/FlightResultsList.tsx` реализован контейнер сообщений с плавной прокруткой (`max-h-[380px] custom-scrollbar`) и автоскроллом к последнему сообщению.
  - В `lib/mockStorage.ts` настроен фильтр 90 дней (`NINETY_DAYS_MS`) и лимит ровно на 10 последних активных диалогов для клиентского отображения с сохранением полной базы в хранилище.
- **Статус:** Реализовано в v8.23.0.

### ADR-058: Устранение дефектов удержания контекста, обогащение карточек билетов и корпоративные тарифы для 6+ пассажиров
- **Контекст:** Устранить ложное предупреждение «Уточните город или страну назначения» при вводе только дат («9-20 октября») при наличии контекста городов, гарантировать мгновенный сброс строки поиска, добавить визуальные бейджи дат, класса и багажа в карточки и реализовать корпоративную логику для 6+ пассажиров.
- **Решение:**
  - В `components/AIInputBar.tsx` исправлена синхронизация `initialQuery` (устранено игнорирование пустой строки `''`) и обеспечена гарантированная очистка инпута в `handleSubmit`.
  - В `lib/nlpParser.ts` исправлена проверка `isDestKnown`: теперь учитывается `previousParams.destinationCity`, устраняя ложные предупреждения.
  - В `components/FlightCard.tsx` добавлены обязательные бейджи:
    - Точные даты рейса: `📅 9 окт – 20 окт (11 дн.)`.
    - Класс обслуживания: `🎫 Эконом`, `✨ Премиум` или `💎 Бизнес`.
    - Багаж: `🧳 Багаж 23 кг` или `🎒 Ручная кладь`.
    - Корпоративный тариф: `🏢 Корпоративный тариф`.
  - Для групп от 6 пассажиров (`passengersCount >= 6`):
    - ИИ формулирует вежливый вопрос о форме оформления (юрлицо / физлицо).
    - В чате выводятся кнопки быстрого ответа: `[👤 Как физлицо]` и `[🏢 Как юрлицо (Корпоративный счет)]`.
    - При выборе юрлица активируется флаг `isCorporateAccount = true`, и билеты помечаются корпоративными бейджами.
- **Статус:** Реализовано в v8.24.0.

### ADR-059: Проактивный диалог ИИ Консьержа и чистое отображение сегментов в карточках билетов
- **Контекст:** Внедрить проактивное уточнение параметров (тип поездки: в одну сторону / обратно через 7 или 14 дней, пассажиры, класс, багаж) через интерактивные кнопки быстрых ответов и исключить дублирование названий городов в маршрутной строке карточек.
- **Решение:**
  - В `lib/nlpParser.ts` и `app/api/search/route.ts` реализован генератор `quickReplies` с категориями (`tripType`, `passengers`, `luggageClass`, `corporate`).
  - Добавлен относительный парсер дат возврата (`обратно через X дней`, `обратно DD месяц`, `в одну сторону`).
  - В `components/FlightResultsList.tsx` внутри диалогового сообщения ИИ встроены интерактивные кнопки быстрых ответов, мгновенно отправляющие команду в чат и обновляющие билеты.
  - В `components/FlightCard.tsx` исправлено формирование `fullRoutePath`: для прямых рейсов отображается `${originCity} ➔ ${destinationCity} (Прямой рейс)`, а для рейсов со стыковкой — только промежуточные хабы без дублей.
- **Статус:** Реализовано в v8.25.0.

### ADR-060: Устранение принудительного скролла страницы, добавление опции «Свой вариант», разделение классов и багажа, удаление лишних плашек
- **Контекст:** Устранить скачок всей страницы вниз к карточкам при отправке сообщений, добавить кнопку «Свой вариант» для дат и числа пассажиров, разделить блоки класса и багажа на два независимых раздела и удалить плашку «О технологии Split-Ticketing» из всех карточек.
- **Решение:**
  - В `components/FlightResultsList.tsx` метод `scrollIntoView` для всей страницы заменен на локальный скролл контейнера сообщений (`chatContainerRef.current.scrollTo`). Страница пользователя остается в стабильном положении без резких скачков.
  - В категории `tripType` и `passengers` добавлены кнопки `[✏️ Свой вариант]` с автофокусом поля ввода через событие `focus-ai-input`.
  - Разделы «Класс» и «Багаж» разделены на два независимых блока:
    - **КЛАСС ОБСЛУЖИВАНИЯ**: `[🎫 Эконом]`, `[✨ Комфорт (Премиум-эконом)]`, `[💎 Бизнес-класс]`, `[👑 Первый класс]`.
    - **БАГАЖ**: `[🎒 Только ручная кладь]`, `[🧳 С багажом (23 кг)]`, `[🧳🧳 2 места багажа]`.
  - В `components/FlightCard.tsx` полностью удалена плашка/кнопка `splitTitle` («О технологии Split-Ticketing» / «Раздельная выписка»).
- **Статус:** Реализовано в v8.26.0.

### ADR-061: Исправление ReferenceError (переменная переводов t) в FlightResultsList
- **Контекст:** Устранить ошибку выполнения `ReferenceError: t is not defined` в `components/FlightResultsList.tsx`.
- **Решение:**
  - В начале компонента `FlightResultsList` объявлена переменная `const t = TRANSLATIONS[language] || TRANSLATIONS.ru;`.
  - Удален неиспользуемый ref `<div ref={chatBottomRef} />`.
  - Проверена работоспособность клиентской части `http://localhost:3000/` со статусом 200 OK.
- **Статус:** Реализовано в v8.27.0.

### ADR-062: Исправление ReferenceError (primaryCabin) и нормализация проверки классов в FlightCard
- **Контекст:** Устранить ошибку `ReferenceError: primaryCabin is not defined` в `components/FlightCard.tsx` и обеспечить безопасную обработку всех свойств объекта `Flight`.
- **Решение:**
  - В `components/FlightCard.tsx` объявлена переменная:
    `const primaryCabin = flight.cabinClass || flight.cabin || flight.segments?.[0]?.cabinClass || 'Economy';`
  - Реализован регистронезависимый поиск классов: `isFirst`, `isBusiness`, `isPremium`.
  - Все обращения к `flight.pricing`, `flight.segments` и `flight.transit` снабжены безопасными операторами optional chaining (`?.`) и значениями по умолчанию.
- **Статус:** Реализовано в v8.28.0.

### ADR-063: Полный рефакторинг логики ИИ Консьержа (Gemini Live State, динамические вопросы и даты)
- **Контекст:** Реализовать строгий контракт Gemini API с массивом `missingFields`, динамическое скрытие отвеченных вопросов, поддержку числовых дат (14.09 ➔ 2026-09-14) и раздельный расчет тарифов с багажом / без багажа.
- **Решение:**
  - В `app/api/search/route.ts` настроен строгий системный промпт для Gemini 2.0 Flash со схемой `missingFields: ['tripType', 'passengers', 'cabinClass', 'luggage']`.
  - В `lib/nlpParser.ts` реализован парсер числовых дат (`14.09` ➔ `2026-09-14`), исправлена нормализация точек и добавлено извлечение Лондона (MOW ➔ LON/LHR).
  - В `components/FlightResultsList.tsx` реализовано динамическое скрытие каждого блока вопросов по мере получения ответа пользователя (`!missingFields.includes(...)`).
  - В `components/FlightCard.tsx` модернизирована функция `formatFlightDates` (чистое форматирование «14 сен 2026» без сбоев), добавлены бейджи ручной клади и багажа.
  - В `lib/mockFlights.ts` внедрены европейские транзитные хабы (Стамбул IST, Дубай DXB, Баку GYD), точные ISO-даты и дифференцированный расчет цен для тарифов без багажа.
### ADR-064: Накопительная память диалога (accumulatedSearchParams), панель фильтров/сортировки и пагинация билетов по 10 штук
- **Контекст:** Реализовать полноценный поиск FlightSaver: удержание накопительной памяти диалога (`accumulatedSearchParams`), мгновенную клиентскую фильтрацию и сортировку, расширенный каталог на 16 рейсов и плавную подгрузку билетов порциями по 10 штук.
- **Решение:**
  - **Накопительная память диалога (`AccumulatedSearchParams`):**
    - В `lib/types.ts` определен интерфейс `AccumulatedSearchParams` (`origin`, `originName`, `destination`, `destinationName`, `departureDate`, `returnDate`, `isOneWay`, `passengers`, `cabinClass`, `hasLuggage`).
    - В `app/page.tsx` состояние `accumulatedSearchParams` передается в `/api/search` при каждом поисковом сообщении и обновляется из ответа сервера `data.accumulatedSearchParams`. При сбросе поиска память очищается.
    - В `app/api/search/route.ts` реализовано объединение данных из Gemini и памяти: отвеченные пользователем параметры исключаются из `missingFields` навсегда.
  - **Панель фильтров и сортировки (`FlightResultsList.tsx`):**
    - Расположена между окном диалога и карточками билетов в премиальном стиле `liquid-glass`.
    - Сортировка: `[✨ Оптимальные (цена/время)]`, `[⚡ Сначала дешевые]`, `[⏱️ Самые быстрые]`, `[🏨 С отелем STPC]`.
    - Фильтр пересадок: `[Все рейсы]`, `[Только прямые]`, `[1 пересадка]`, `[Отель STPC]`.
    - Фильтр времени вылета: `[Любое]`, `[🌅 Утро (06:00-12:00)]`, `[☀️ День (12:00-18:00)]`, `[🌙 Вечер (18:00-00:00)]`.
    - Мгновенная клиентская пересортировка и фильтрация без перезагрузки страницы через `useMemo`.
  - **Пагинация и подгрузка билетов (по 10 штук):**
    - Каталог в `lib/mockFlights.ts` расширен до 16 разнообразных рейсов с метаданными длительности в минутах (`totalDurationMinutes`), времени вылета (`departureTimeOfDay`), числа пересадок (`stopsCount`) и признака STPC.
    - По умолчанию выводится 10 билетов со счетчиком `Показано X из Y`.
    - Внизу списка размещена кнопка `[Показать еще 10 билетов]`, дозагружающая следующие варианты. Если билетов <= 10, кнопка скрывается.
  - **Карточки билетов (`FlightCard.tsx`):**
    - Добавлен бейдж количества пассажиров: `[👤 {passengers} пасс.]`.
    - Обеспечено визуальное единообразие бейджей дат, класса (`[🎫 Эконом]`, `[✨ Комфорт]`, `[💎 Бизнес]`, `[👑 Первый класс]`) и багажа (`[🎒 Только ручная кладь]` / `[🧳 Багаж 23 кг]`).
- **Статус:** Реализовано в v8.30.0.

### ADR-065: Прямой вызов Gemini Flash, реалистичная маршрутная сетка (РФ ➔ Европа без прямых рейсов) и протокол самопроверки
- **Контекст:** Провести критический рефакторинг: исключить самодельные regex-парсеры в пользу прямого вызова Gemini с накопительной памятью, запретить прямые рейсы из РФ в Европу, настроить реалистичную сетку через хабы (Стамбул, Дубай, Ереван, Баку, Доха, Белград) и закрывать вопрос об обратном билете при вводе даты возврата (например, 29.10).
- **Решение:**
  - **Системный контракт Gemini (`app/api/search/route.ts`):**
    - "Питер / СПб / Санкт-Петербург" $\rightarrow$ строго origin: "Санкт-Петербург" [LED].
    - "Мюнхен" $\rightarrow$ строго destination: "Мюнхен" [MUC].
    - "20 октября" $\rightarrow$ `departureDate: "2026-10-20"`.
    - "29.10" $\rightarrow$ `returnDate: "2026-10-29"`, `isOneWay: false`.
    - При наличии даты возврата вопрос о типе поездки (`tripType`) закрывается навсегда.
    - Массив `missingFields` содержит только реально незаполненные поля.
  - **Реалистичная маршрутная сетка (`lib/mockFlights.ts`):**
    - Для рейсов из РФ в Европу (LED/MOW $\rightarrow$ MUC/BER/CDG/FCO/LON/etc.) полностью исключены прямые рейсы (`stopsCount === 0` $\rightarrow$ 0 вариантов).
    - Все 16 маршрутов следуют со стыковками через международные хабы:
      1) Стамбул [IST/SAW] (Turkish Airlines / Pegasus)
      2) Дубай [DXB] (Flydubai / Emirates) со включенным отелем STPC 4★ при стыковке от 8ч
      3) Ереван [EVN] (FlyOne Armenia / Lufthansa)
      4) Доха [DOH] (Qatar Airways)
      5) Баку [GYD] (Azerbaijan Airlines / Lufthansa)
      6) Белград [BEG] (Air Serbia / Lufthansa)
    - Время перелета: реалистичное (от 7ч 30м до 18ч 30м с учетом пересадок).
  - **UX пагинации (`components/FlightResultsList.tsx`):**
    - Убраны любые вызовы `scrollTo`/`scrollIntoView` при нажатии кнопки «Показать еще 10 билетов». Положение экрана пользователя сохраняется на месте клика.
  - **Автоматизированный протокол самопроверки (`scratch/verify_protocol.js`):**
    - Успешно выполнены 5 сквозных тестов: фиксация LED $\rightarrow$ MUC (20.10), обратный билет (29.10), класс Economy, 1 пассажир и проверка 16 стыковочных рейсов без прямых.
- **Статус:** Реализовано в v8.31.0.

### ADR-066: Полное удаление заглушек, прямая интеграция Gemini и динамическая генерация билетов для любых направлений мира
- **Контекст:** Полная зачистка кодовой базы от дефолтных городов-заглушек (Москва, Бангкок, MOW, LON, BKK). Прямая интеграция Gemini в `/api/search` с поддержкой любого направления в мире (например, Екатеринбург `[SVX]` ➔ Конго/Браззавиль `[BZV]`), сохранение контекста в диалоге и генерация карточек строго под реальные города.
- **Решение:**
  - **Тотальная зачистка от заглушек:**
    - Из `lib/mockFlights.ts` и `lib/nlpParser.ts` удалены жестко зашитые дефолтные подстановки `originCity || 'Москва'`, `destCity || 'Лондон'`, `originIata || 'MOW'`. Если город не определен — возвращается пустой список и запрос на уточнение, а не фиктивная Москва.
  - **Маршрут `/api/search`:**
    - Прямой вызов Gemini Flash (`gemini-flash-latest`, `gemini-3.6-flash`, `gemini-3.7-flash`) с системным промптом, извлекающим origin/destination, даты в ISO, класс, багаж, число пассажиров и массив `missingFields`.
    - Поддержка накопительного состояния: при уточнении («11 ноября», «эконом») ранее определенные города (Екатеринбург ➔ Конго) сохраняются.
  - **Динамическая генерация карточек билетов (`generateDynamicFlights`):**
    - Билеты генерируются исключительно под распознанный маршрут (`parsed.origin` ➔ `parsed.destination`), например `Екатеринбург [SVX] ➔ Стамбул [IST] ➔ Браззавиль [BZV]` с реальными ценами, багажом и программами отелей STPC 4★.
  - **Верификация:**
    - Запрос «Екатеринбург конго 17 октября» $\rightarrow$ `origin: SVX (Екатеринбург)`, `destination: BZV (Браззавиль (Конго))`, `departureDate: 2026-10-17`.
    - Уточнение «11 ноября» $\rightarrow$ `returnDate: 2026-11-11`, города сохранены, `tripType` закрыт.
    - Уточнение «эконом» $\rightarrow$ `cabinClass: economy`, класс зафиксирован.
- **Статус:** Реализовано в v8.32.0.

### ADR-067: Чистый обработчик Gemini 2.0 Flash, устранение дефолтов в nlpParser и регламент отчетов в PROJECT JOURNAL TEMPLATES
- **Контекст:** Окончательно удалить все зашитые значения городов из `lib/nlpParser.ts`, `app/page.tsx` и `app/api/search/route.ts`. Внедрить чистый обработчик Gemini с поддержкой параметров `noFlightsFound`, отсутствия рейсов при неполных городах и корректной генерацией карточек. Перевести все отчеты в папку `PROJECT JOURNAL TEMPLATES` с регламентом суточного объединения.
- **Решение:**
  - **Серверный роут `/api/search`:**
    - Интегрирован чистый обработчик Gemini со схемой `missingFields: ["origin", "destination", "departureDate", "tripType", "passengers", "cabinClass", "luggage"]`, признаком `noFlightsFound` и динамическими билетами строго под реальные распознанные города.
    - При отсутствии одного или обоих городов (`!parsed.origin || !parsed.destination`) рейсы не генерируются (`flights: []`), пользователю возвращается запрос на указание недостающего направления.
  - **Зачистка `lib/nlpParser.ts`:**
    - Устранены последние скрытые дефолты Москвы и Бангкока на строках 490/505, исключен сбой `toLowerCase` при значении `null`/`undefined`.
    - Добавлен город Парма `[PMF]`.
  - **Фронтенд `app/page.tsx`:**
    - Инициализация состояния строго в `null` (`origin: null`, `destination: null`).
  - **Регламент отчетов:**
    - Все отчеты хранятся исключительно в `PROJECT JOURNAL TEMPLATES` под именем `Report_v<день>_<номер_отчета>.md` (например, `Report_v8_33.md`).
    - В конце дня выполняется автоматическое суточное объединение в `Report_v<день>.md`.
- **Статус:** Реализовано в v8.33.0.

### ADR-068: Восстановление пакета @types/node и явная привязка typeRoots в tsconfig.json
- **Контекст:** Ошибка VSCode / TypeScript Language Server: `Cannot find type definition file for 'node'`. В папке `node_modules/@types/node` отсутствовали декларации типов `index.d.ts`.
- **Решение:**
  - Восстановлен полный пакет `@types/node` (включая все встроенные модули Node.js и глобальные типы) в директорию `FlightSaver/node_modules/@types/node/`.
  - В `FlightSaver/tsconfig.json` в `compilerOptions` явно добавлен параметр `"typeRoots": ["./node_modules/@types"]`.
  - Изменения зафиксированы и отправлены в ветку `main` в GitHub.
- **Статус:** Реализовано в v8.34.0.

### ADR-069: Логика живых ответов Gemini aiResponse, строгий учет пассажиров и zero-scroll пагинация
- **Контекст:** Настроить системный промпт Gemini для генерации развернутого живого ответа `aiResponse`, запретить подстановку пассажиров по умолчанию (если не названы -> `passengers: null` и обязательное добавление в `missingFields`), мгновенно скрывать отвеченные категории вопросов в интерфейсе и гарантировать неподвижность экрана при клике «Показать еще 10 билетов».
- **Решение:**
  - **Системный промпт Gemini (`app/api/search/route.ts`):**
    - Если пассажиры не названы явно $\rightarrow$ `passengers: null`, поле обязательно включается в `missingFields`.
    - Если указана обратная дата $\rightarrow$ `isOneWay: false`, вопрос о возврате закрыт.
    - Выбранный класс (`cabinClass`) и багаж (`hasLuggage`) фиксируются и исключаются из `missingFields`.
    - Формирование развернутого ответа `aiResponse` с пояснением стыковок в хабах и уточнением недостающих параметров.
  - **Накопитель состояния (`app/page.tsx`):**
    - Строгое слияние `accumulatedSearchParams` с сохранением ранее зафиксированных ответов.
    - Вопросы в диалоге отображаются строго по актуальному срезу `missingFields`.
  - **Пагинация без скролла (`components/FlightResultsList.tsx`):**
    - Кнопка «Показать еще 10 билетов» использует `(e) => { e.preventDefault(); e.stopPropagation(); setVisibleCount(prev => prev + 10); }` без любых вызовов `scrollIntoView`/`scrollTo`.
- **Статус:** Реализовано в v8.35.0.

### ADR-070: Восстановление модульных деклараций Next.js и чистая компиляция TypeScript
- **Контекст:** Ошибка VSCode / IDE `@[current_problems]`: `Cannot find module 'next/navigation' or its corresponding type declarations.` в файле `app/page.tsx`. В `node_modules/next/` отсутствовали файлы экспорта деклараций верхнего уровня (`navigation.d.ts`, `server.d.ts`, `link.d.ts`, `headers.d.ts`, `image.d.ts` и папка `types/`).
- **Решение:**
  - Полностью восстановлены модульные декларации и типы `next` (`navigation.d.ts`, `server.d.ts`, `link.d.ts`, `headers.d.ts`, `image.d.ts`, `next/types`), `@supabase/ssr`, `tailwindcss` в `FlightSaver/node_modules/`.
  - Запущена проверка компилятором `tsc --noEmit` — получено 0 ошибок (код выхода 0).
  - Изменения зафиксированы в репозитории.
- **Статус:** Реализовано в v8.36.0.

### ADR-071: Перевод FlightSaver на 100% живого ИИ-агента Gemini с полным рассуждением и устойчивостью к опечаткам
- **Контекст:** Интеграция прямого агента Gemini в `/api/search` с живым диалогом `replyText`, устойчивостью к опечаткам («Люксенбург», «Екатиринбург», «Мунхен»), рассуждением об особенностях маршрута и генерацией интерактивных кнопок `missingQuestions` для недостающих полей.
- **Решение:**
  - **Системная инструкция Gemini (`app/api/search/route.ts`):**
    - Распознавание опечаток и разговорных названий городов (`Самара` [KUF], `Люксембург` [LUX], `Екатеринбург` [SVX], `Мюнхен` [MUC], `Санкт-Петербург` [LED], `Париж` [CDG], `Рим` [FCO]).
    - Автоматический учет стыковок через Стамбул [IST] или Дубай [DXB] для европейских и транзитных направлений.
    - Возврат структурированного JSON с полями `replyText`, `data` и массивом `missingQuestions` с текстами вопросов и вариантами ответов.
  - **Фронтенд (`app/page.tsx` и `components/FlightResultsList.tsx`):**
    - Отображение живого текста `replyText` в диалоге помощника.
    - Динамический рендеринг интерактивных кнопок `missingQuestions`.
    - Отправка выбранных вариантов в историю сообщений с мгновенным пересчетом билетов.
  - **Верификация TypeScript:** Проверка `tsc --noEmit` пройдена без ошибок (код 0).
- **Статус:** Реализовано в v8.37.0.

### ADR-072: Включение настоящего ИИ-агента Gemini с Google Search Grounding и динамической генерацией рейсов
- **Контекст:** Переход на чистый агентский вызов Gemini 2.0 Flash в `app/api/search/route.ts` с поддержкой Google Search Grounding, формированием ответов `reply`, точных IATA-кодов городов, расчетом отелей STPC 4★ и динамических карточек рейсов.
- **Решение:**
  - В `app/api/search/route.ts` внедрен системный промпт живого ИИ-консьержа с использованием поиска Google для точного разрешения IATA-кодов и исправления любых опечаток.
  - Структурированный ответ содержит `reply`, `origin`, `originCity`, `destination`, `destinationCity`, `departureDate`, `returnDate`, `isOneWay`, `passengers`, `cabinClass`, `hasLuggage`, `missingQuestions`.
  - Генерация динамических билетов `generateDynamicFlights` возвращает полноценные объекты рейсов (Turkish Airlines через Стамбул IST и Emirates через Дубай DXB с отелем STPC 4★).
  - Обеспечен отказоустойчивый fallback при временных лимитах квоты Gemini API.
- **Статус:** Реализовано в v8.39.0.

### ADR-073: Надежный вызов Gemini с обработкой географии, currentParams и live reasoning output
- **Контекст:** Оптимизировать полезную нагрузку (payload) вызова Gemini: передача `currentParams`, `history` и нового сообщения пользователя в едином контексте, строгое сопоставление сложных городов (Челябинск -> CEK, Монако -> NCE), сохранение ранее определенных параметров и мгновенный вывод живого текста `reply` и карточек билетов на фронтенде.
- **Решение:**
  - В `app/api/search/route.ts` обновлен системный промпт: точная обработка географии (Челябинск [CEK], Монако/Ницца [NCE], Самара [KUF], Лос-Анджелес [LAX], Люксембург [LUX]), сохранение параметров из `currentParams`, форматирование дат `YYYY-MM-DD` и динамические `missingQuestions`.
  - В `app/page.tsx` обеспечено моментальное отображение `replyText` / `reply` в сообщении ассистента и мгновенная передача `response.flights` в состояние компонента.
  - Верифицирована компиляция TypeScript (0 ошибок).
- **Статус:** Реализовано в v8.40.0.

### ADR-074: Подключение Google Search Grounding и Structured Output к Gemini API
- **Контекст:** Интегрировать инструмент `tools: [{ googleSearch: {} }]` в серверный роут `app/api/search/route.ts` для поиска информации в Google в реальном времени, разрешения городов без собственных аэропортов (Монако -> Ницца NCE) и возврата `groundingMetadata`.
- **Решение:**
  - В роуте `app/api/search/route.ts` подключена поддержка `tools: [{ googleSearch: {} }]` и `generationConfig: { responseMimeType: 'application/json' }`.
  - Системная инструкция инструктирует ИИ-консьержа использовать ближайшие международные хабы при отсутствии аэропорта в городе и подбирать составные маршруты Split-Ticketing с отелями STPC.
  - Ответ сервера возвращает `replyText`, `text`, `parsed`, `flights` и `groundingMetadata`.
- **Статус:** Реализовано в v8.41.0.

### ADR-075: Накопление и сквозная передача сообщений и searchState на клиенте
- **Контекст:** Реализовать сохранение контекста, истории диалога `messages` (`role: 'user' | 'model'`, `parts: [{ text }]`) и параметров поиска `searchState` на клиенте с их сквозной передачей на сервер `/api/search`.
- **Решение:**
  - В `app/page.tsx` функция `handlePerformSearch` обновлена для поддержки параметров `searchState` (`origin`, `destination`, `passengers`, `cabinClass`, `hasLuggage` / `baggage`) и формирования массива `messages`.
  - При получении ответа `data.text` / `data.replyText` ответ ИИ ассистента (`role: 'assistant' / 'model'`) сохраняется в истории диалога.
  - Проверена компиляция TypeScript (0 ошибок).
- **Статус:** Реализовано в v8.42.0.

### ADR-076: Передача распарсенных параметров и структурированная генерация вариантов перелета в Gemini API
- **Контекст:** Реализовать структурированный ответ Gemini со схемой `summaryText`, `parsedParams` (origin, originIata, destination, destinationIata, departureDate, returnDate, passengers, cabinClass, baggage) и массивом `flights` (routeTitle, duration, airlines, price, marketPrice, savingsAmount, hasStpcHotel, stpcDetails) строго под запрошенные города.
- **Решение:**
  - В `app/api/search/route.ts` обновлена системная инструкция с правилами извлечения параметров и подбора составных маршрутов со Split-Ticketing пересадками (Стамбул IST, Дубай DXB) и отелями STPC 4★.
  - Функция `enrichFlights` генерирует полноценные объекты рейсов, поддерживая как переданные от Gemini данные, так и fallback-варианты при временных лимитах квот.
  - Верифицирована компиляция TypeScript (0 ошибок).
- **Статус:** Реализовано в v8.43.0.

### ADR-077: Архитектура ИИ-Консьержа с контролем 5 обязательных параметров и динамическими статусами
- **Контекст:** Внедрить архитектуру ИИ-консьержа в `/api/search`: валидация 5 обязательных параметров (тип маршрута, маршрут откуда/куда, даты вылета/возврата, число пассажиров, багаж), разделение состояний на `needs_clarification` (вежливое персонализированное уточнение без шаблонов, пустой массив `flights: []`) и `ready` (генерация 2-3 реалистичных маршрутов Split-Ticketing с отелями STPC).
- **Решение:**
  - В `app/api/search/route.ts` настроен системный промпт с 5 обязательными параметрами и статусами `needs_clarification` / `ready`.
  - Массив `missingParams` возвращает конкретный список недостающих полей.
  - При `status === 'ready'` генерируются и обогащаются карточки рейсов, при `status === 'needs_clarification'` массив `flights` остается пустым.
  - В `app/page.tsx` поддержан вывод `data.message` и сохранение параметров.
- **Статус:** Реализовано в v8.44.0.

### ADR-078: Клиентский компонент диалога ConciergeChat с удержанием searchState и рендерингом рейсов
- **Контекст:** Реализовать компонент `components/ConciergeChat.tsx` для управления диалогом ИИ Консьержа: накопление сообщений `messages: [{ role, parts: [{ text }] }]`, динамическое обновление `searchState` и условный рендеринг карточек перелетов только при готовности всех 5 параметров (`status === 'ready'`).
- **Решение:**
  - Создан компонент `components/ConciergeChat.tsx`.
  - Реализован обработчик отправки `handleSendMessage`, реактивное обновление `searchState` и отображение индикатора загрузки с анимацией пульсации.
  - Настроен рендеринг карточек перелетов с ценой, экономией Split-Ticketing и бейджем бесплатного 4★ отеля STPC.
  - Верифицирована компиляция TypeScript (0 ошибок).
- **Статус:** Реализовано в v8.45.0.

### ADR-079: Интеграция зависимости @google/genai и синхронизация GEMINI_API_KEY
- **Контекст:** Добавить зависимость `@google/genai` в `package.json` проекта FlightSaver для развертывания на Vercel и зафиксировать конфигурацию ключа `GEMINI_API_KEY`.
- **Решение:**
  - В `package.json` добавлена зависимость `"@google/genai": "^0.1.1"`.
  - Ключ `GEMINI_API_KEY` верифицирован в локальном окружении (`.env.local`) и передан в переменные окружения Vercel.
  - Верифицирована компиляция TypeScript (0 ошибок).
- **Статус:** Реализовано в v8.46.0.

### ADR-080: Строгое извлечение городов и запрет вывода технических переменных в диалоге ИИ Консьержа
- **Контекст:** Настроить системный промпт ИИ Консьержа для точного извлечения городов вылета/прилета (Минск MSQ, Сиэтл SEA, Сургут SGC, Манчестер MAN, Челябинск CEK, Монако NCE), дат и параметров, с категорическим запретом вывода технических названий переменных (`tripType`, `route`, `dates`, `passengers`, `baggage`) в диалоговом ответе пользователю.
- **Решение:**
  - В `app/api/search/route.ts` обновлен `systemPrompt`: общение строго на естественном русском языке.
  - При наличии только городов и даты вылета ИИ формирует вежливое подтверждение («Принято: Минск → Сиэтл на 11 сентября 2026 года») и мягко уточняет тип билета, число пассажиров и наличие багажа.
  - При сборе всех параметров (`status: "ready"`) генерируется структурированный массив `flights`.
  - Верифицирована компиляция TypeScript (0 ошибок).
- **Статус:** Реализовано в v8.47.0.

### ADR-081: Очистка начального состояния и устранение фантомных дефолтных городов на клиенте и сервере
- **Контекст:** Гарантировать, что начальное состояние в `ConciergeChat.tsx` и `/api/search/route.ts` является абсолютно пустым (`searchState: {}`, `messages: []`, без привязки к Челябинску или другим городам), а параметры заполняются исключительно из реальных реплик пользователя и ответа Gemini.
- **Решение:**
  - В `components/ConciergeChat.tsx` подтверждены чистые начальные состояния `useState<Record<string, any>>({})` и `useState([])`.
  - В `app/api/search/route.ts` устранены статические подстановки городов в fallback-объектах (`origin: prev.origin || null`, `destination: prev.destination || null`).
  - Верифицирована компиляция TypeScript (0 ошибок).
- **Статус:** Реализовано в v8.48.0.

### ADR-082: Поддержка путей src/app и синхронизация репозитория GitHub
- **Контекст:** Предоставить зеркальные пути `src/app/api/search/route.ts` и `src/components/ConciergeChat.tsx` для обратной совместимости, зафиксировать коммит `fix: natural conversational Russian and dynamic city slot filling` и отправить изменения в ветку `main` GitHub.
- **Решение:**
  - Созданы зеркальные модули `src/app/api/search/route.ts` и `src/components/ConciergeChat.tsx` с прямой реэкспортировкой.
  - Проверена компиляция TypeScript (0 ошибок).
  - Выполнен коммит и пуш в GitHub `main`.
- **Статус:** Реализовано в v8.49.0.

### ADR-083: Универсальный обработчик ИИ-Консьержа с автоопределением мировых городов и естественным русским языком
- **Контекст:** Развернуть универсальный обработчик в `app/api/search/route.ts` и `src/app/api/search/route.ts` для любых городов/стран мира с автоматическим извлечением 3-буквенных IATA-кодов, дат (включая естественные фразы), типа поездки, пассажиров и багажа (`cabin_only` / `checked_baggage`).
- **Решение:**
  - Настроена системная инструкция ИИ-Консьержа FlightSaver: естественный русский язык, строгий запрет технических терминов и шаблонов.
  - При неполных параметрах (`status: "needs_clarification"`) возвращается живой диалоговый ответ и пустой массив `flights: []`.
  - При всех 5 параметрах (`status: "ready"`) генерируются реалистичные Split-Ticketing маршруты с отелями STPC при стыковках от 8 часов.
  - Проверена компиляция TypeScript (0 ошибок).
- **Статус:** Реализовано в v8.50.0.

### ADR-084: Клиентский интерфейс ConciergeChat без зашитых городов и моков
- **Контекст:** Очистить компонент `components/ConciergeChat.tsx` от любых встроенных моков или зашитых городов, сделав его легким интерфейсом для отправки реплик в формате `{ role, parts: [{ text }] }` и рендеринга живых ответов Gemini и найденных рейсов.
- **Решение:**
  - `components/ConciergeChat.tsx` переписан: начальное состояние `messages: []`, `flights: []`.
  - Отправка сообщений в формате `apiPayload: newMessages.map(m => ({ role, parts: [{ text }] }))`.
  - При `status === 'ready'` и наличии билетов выполняется адаптивный рендеринг карточек перелетов с ценой, экономией и STPC отелем.
  - Проверена компиляция TypeScript (0 ошибок).
- **Статус:** Реализовано в v8.51.0.

### ADR-085: Универсальный zero-mock Gemini авиаконсьерж и боевой деплой
- **Контекст:** Зафиксировать финальную сборку универсального zero-mock авиаконсьержа FlightSaver на базе Google Gemini 2.0 Flash (`src/app/api/search/route.ts` и `src/components/ConciergeChat.tsx`) и отправить изменения в боевой деплой Vercel.
- **Решение:**
  - Полностью проверена работа компонентов без статических моков.
  - Верифицирована компиляция TypeScript (0 ошибок).
  - Выполнен коммит `feat: universal zero-mock Gemini flight concierge` и push в ветку `main`.
- **Статус:** Реализовано в v8.52.0.

### ADR-086: Вынос systemInstruction в config и устранение 400 role sequence error
- **Контекст:** Устранить ошибку HTTP 400 (недопустимая последовательность ролей), вынеся `systemInstruction` из массива `contents` в конфигурацию вызова (`system_instruction`), и обеспечив строгое чередование ролей `user` / `model` в `formattedContents`.
- **Решение:**
  - В `app/api/search/route.ts` системная инструкция перенесена в поле `system_instruction`.
  - Массив `messages` валидируется и преобразуется строго в формат `{ role: 'model' | 'user', parts: [{ text }] }`.
  - Проверена компиляция TypeScript (0 ошибок).
- **Статус:** Реализовано в v8.53.0.

### ADR-087: Универсальный парсер входящих запросов и прямая REST-интеграция Gemini
- **Контекст:** Реализовать универсальный парсер полезной нагрузки (поддержка `query`, `text`, `message`, `messages`, прямой строки) и прямой вызов Gemini REST API со структурированным JSON ответом.
- **Решение:**
  - В `app/api/search/route.ts` реализовано извлечение текста из любых типов запросов.
  - Настроен прямой вызов Gemini REST API со структурированной схемой `searchState` и `flights`.
  - Верифицирована компиляция TypeScript (0 ошибок).
- **Статус:** Реализовано в v8.54.0.

### ADR-088: Замена мокового поискового движка на главный живой компонент ConciergeChat в app/page.tsx
- **Контекст:** Переключить главную страницу `app/page.tsx` (и `src/app/page.tsx`) на прямой рендеринг компонента `ConciergeChat` с живым вызовом Gemini API вместо устаревшего mock-движка.
- **Решение:**
  - Обновлен `app/page.tsx` и создан `src/app/page.tsx`: лаконичный заголовок «Умный поиск перелётов одной фразой» и прямой рендеринг `<ConciergeChat />`.
  - Верифицирована компиляция TypeScript (0 ошибок).
  - Выполнен коммит `refactor: replace mock flight engine with live Gemini Concierge` и push в ветку `main`.
- **Статус:** Реализовано в v8.55.0.





















